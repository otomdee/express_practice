<html>
  <head>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <%= message %>

    <ul>
      <% users.forEach((user) => { %>
        <%- include('users', {user: user}); %>
      <% }); %>
    </ul>

    <%- include('footer') %>
  </body>
</html>
